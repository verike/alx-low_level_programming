#include "main.h"

/**
 * _atoi - filters a string with integers and returns only the integer
 *
 * @s: String passed in as an argument
 *
 * Return: Returns the interger stored in the variable 'c'
 */
int _atoi(char *s)
{
	int c;

	c = atoi(s);

	return (c);

}
