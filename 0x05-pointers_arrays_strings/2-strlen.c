#include "main.h"

/**
 * _strlen - checks the length of a string
 *
 * @s: Char argument passed
 *
 * Return: Len the result of the program
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);

}
