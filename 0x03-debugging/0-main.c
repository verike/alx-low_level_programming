#include "main.h"

/**
 * main - Tests function that checks if an integer is positive or negative
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);

}
