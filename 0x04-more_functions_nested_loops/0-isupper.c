#include "main.h"

/**
 * _isupper - returns 1 if value is upper case and 0 if else
 *
 * @c: charater passed in as an argument
 */
int _isupper(int c)
{
	r = isupper(c);

	if (r != 0)
	{
		return (1);

	}
	else
	{
		return (0):

	}
}
