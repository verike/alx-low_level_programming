#include "main.h"

/**
 * _abs - returns the absolute value of the argument passed into it
 *
 * @n: argument passed
 *
 * Return: 'a' which is the absolute value
 */
int _abs(int n)
{
	int a;

	a = abs(n);

	return (a);

}

